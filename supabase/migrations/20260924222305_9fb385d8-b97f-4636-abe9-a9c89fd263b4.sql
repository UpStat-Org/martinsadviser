-- Intentionally empty.
--
-- This version was generated by Lovable when it re-applied the migrations
-- below to its hosted database. Its body was a verbatim copy of them, so on
-- any database where they already ran it was redundant, and on a fresh
-- database it failed (e.g. duplicate constraints). The version is kept so the
-- migration history of existing deployments still matches this directory.
--
-- Replayed migrations (still applied, in order, from their own files):
--   20260916180000_harden_portal_upload_paths.sql
--   20260916190000_narrow_portal_read_surface.sql
SELECT 1;
